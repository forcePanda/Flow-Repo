({
    /* When a flow executes this component, it calls the invoke method */
    invoke : function(component, event, helper) {
       /* This is just a dummy action, so we can just keep it blank. */
       //console.log('Dummy flow action called.');
    }
 })